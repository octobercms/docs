---
subtitle: Столбец списка
shortname: Partial
---
# Столбец Partial

Столбец `partial` рендерит содержимое столбца с использованием частичного представления или файла представления. Значение `path` может ссылаться на файл частичного представления, иначе имя столбца используется как имя представления. Область видимости по умолчанию для пути представления — путь представлений контроллера.

```yaml
content:
    label: Content
    type: partial
    path: content_column
```

Поддерживаются следующие свойства.

Свойство | Описание
------------- | -------------
**path** | путь к [файлу частичного представления](../../extend/system/views.md) или [коду шаблона представления](../../extend/services/response-view.md), по умолчанию — имя столбца с префиксом **column_**.

Когда `path` задан как неквалифицированное имя файла (имя файла без пути к директории и расширения), исходный путь определяется в директориях модели или контроллера. Следующий пример будет искать файл частичного представления по пути **../models/mymodel/_column_for_content.php** или **../controllers/mycontroller/_column_for_content.php**.

```yaml
content:
    type: partial
    path: column_for_content
```

Вы можете указать полный `path` для доступа к частичным представлениям вне директорий модели или контроллера. Это может быть полезно для совместного использования частичных представлений между определениями.

```yaml
content:
    label: Content
    type: partial
    path: $/acme/blog/partials/_content_column.php
```

Внутри частичного представления доступны следующие переменные.

- `$value` — значение ячейки по умолчанию.
- `$record` — модель, используемая для ячейки.
- `$column` — сконфигурированный объект класса `Backend\Classes\ListColumn`.

Вот пример содержимого файла **_content_column.php**.

```php
<?php if ($record->is_active): ?>
    <?= e($value) ?>
<?php endif ?>
```

## Использование шаблонов представлений

Вы можете передать код шаблона представления в `path` для доступа к шаблонам сервиса представлений внутри плагина. Следующий код будет найден по пути **plugins/acme/blog/views/listcolumns/content.php**.

```yaml
content:
    label: Content
    type: partial
    path: acme.blog::listcolumns.content
```

:::tip
Путь должен содержать символы `::` для активации сервиса представлений.
:::

#### См. также

::: also
* [Рендеринг представлений контроллера](../../extend/system/views.md)
* [Сервис ответов и представлений](../../extend/services/response-view.md)
:::
